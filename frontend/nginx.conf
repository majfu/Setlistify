server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: let React Router handle client-side routes
    # (e.g. /home, /browse) instead of returning 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
