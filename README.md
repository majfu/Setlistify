SetListify is a web application designed for easy Spotify playlist creation. Instead of manually hunting for setlists and top tracks, SetListify uses Setlist.fm APIs and AI to predict concert sets and lets you build a custom playlist in minutes.
